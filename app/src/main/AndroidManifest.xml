<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.project"
    android:revisionCode="1"
    android:versionCode="1"
    android:versionName="1.1.0">

    <uses-permission android:name="android.permission.ACCES S_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/delly_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:label">
        <activity android:name=".activity_fragments_class.CreatePasswordActivity" />
        <activity android:name=".activity_fragments_class.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity_fragments_class.SettingsActivity"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity android:name=".activity_fragments_class.ExtendedPartnerActivity" />
        <activity android:name=".activity_fragments_class.HomeActivity" />
        <activity android:name=".activity_fragments_class.RulesActivity" />
        <activity android:name=".activity_fragments_class.LoginActivity" />
        <activity android:name=".activity_fragments_class.Registration.SignupActivity_FirstStep" />
        <activity android:name=".activity_fragments_class.Registration.SignupActivity_SecondStep" />
        <activity android:name=".activity_fragments_class.Registration.SignupActivity_ThirdStep" />
        <activity android:name=".activity_fragments_class.Registration.SignupActivity_FourthStep" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_key" />
    </application>

</manifest>